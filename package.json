{
  "name": "tfexpress-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../ecommerce-frontend && npm install",
    "build:frontend": "cd ecommerce-frontend && npm run build",
    "start:backend": "cd backend && npm start",
    "heroku-postbuild": "npm run build:frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  }
}
