{
  "name": "tfexpress-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd ecommerce-frontend && npm install",
    "build": "cd ecommerce-frontend && npm run build",
    "preview": "cd ecommerce-frontend && npm run preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  }
}
